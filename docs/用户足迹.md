# 用户足迹

## 2026-07-21

### 提问
- 获取 https://github.com/SuperCup/Data_Center.git 代码并启动运行

### 处理记录
1. 将仓库克隆到 `/Users/luffy/Desktop/Project/Data_Center`
2. 识别为 Create React App（React + TypeScript + Ant Design）项目，入口在 `data-center/`
3. 执行 `npm install` 安装依赖
4. 执行 `npm start` 启动开发服务器（默认 http://localhost:3000）

### 提问（2026-07-21 15:32）
- 舒洁到家即时零售数据看板 HTML 四项优化：
  1. 增加时间筛选模块
  2. 周报拆分数据增加同环比
  3. 删除大区板块（内部分区暂不使用）
  4. 产品系列下钻（表格展示 + Size 字段）

### 处理记录
- 修改文件：`docs/dashboards/舒洁_到家即时零售_数据看板.html`（同步回原微信临时路径）
- **时间筛选**：控制条新增「本周 / 上周 / 去年同周」切换，联动 KPI、趋势、商品、TOP10 等模块
- **拆分同环比**：选中「本周」时，各 TOP10 表及产品系列表增加「环比」「同比」列；拆分维度 PW/PY 数据由概览比例推导（正式接入后替换为真实数据）
- **删除大区**：移除「区域 TOP10」模块；零售商/省份/城市表去掉「大区」列；分析报告文案改为省份维度
- **产品下钻**：新增「产品系列」表（含同环比），点击系列展开 SKU 明细表，展示 UPC、产品名、Size、子品牌及 GMV 指标

### 提问（2026-07-21 15:32）
- 从 supermingzhao 分支拖一个新分支：shujie

### 处理记录
- 已从 `supermingzhao` 创建并切换到本地分支 `shujie`

### 提问（2026-07-21 21:39）
- 时间筛选需要支持自定义

### 提问（2026-07-23）
- 确认改舒洁看板

### 处理记录
- 目标已确认：`docs/dashboards/舒洁_到家即时零售_数据看板.html`
- 仍待确认：自定义交互形态、同环比口径、自定义区间数据口径

## 2026-07-23

### 用户需求
版面需要增加 AI 元素：基于看板向 AI 查数、经营分析建议、商机探索；含会话管理、品牌知识库、个人行为记忆、产出物管理。参考 Codex / WorkBuddy，保留到店 / 即时零售 / 物码固定看板。

### 拍板结论
1. 先做过独立 HTML 原型，后改为**直接在 data-center React 项目调整**
2. 布局：看板与 AI **切换式**（非融合）
3. 会话记录：**侧栏常驻**（切换 AI 菜单不隐藏）
4. AI 深度：仅 UI + 模拟对话
5. Agent 六能力：数据查询、经营诊断、数据分析、行动建议、商机探索、报告生成
6. UI 需更精细，对齐现有 Ant Design 风格

### 实现位置
- `data-center/src/layouts/ClientLayout.tsx` — 顶栏 Segmented 切换；AI 对话页全幅 WorkBuddy 布局
- `data-center/src/pages/client/ai-workbench/AiWorkbench.tsx` — 参考 WorkBuddy：可折叠左侧会话、居中品牌+能力胶囊+大输入框
- `data-center/src/services/aiMock.ts`
- 路由：`/client/ai`、`/ai/knowledge`、`/ai/memory`、`/ai/artifacts`

### 历史说明
- `docs/dashboards/ai-workbench/` 为早期独立 demo，后续以 React 主应用为准，不再扩展 demo。

### 迭代（同日）
- 统一 AI 全站侧栏（对话/知识库/记忆/产出物同一套菜单）
- 账号仅保留右上角；侧栏去掉用户信息
- 去掉会话框未提及标签（分类胶囊、模型标签、权限脚注等）
- Agent 名称：即时零售增长Agent；客户名：舒洁
- AI 模式顶栏左侧仅保留「数据看板 | AI 工作台」切换，无 logo
- 侧栏默认展开、去掉折叠按钮；会话生成中列表加载动画；支持附件；菜单改为新建会话/品牌知识库/记忆管理/产物仓库/附件仓库；每会话独立产物与附件管理
- **会话记录常驻**：切换知识库/记忆/产物/附件等菜单时，侧栏会话列表不隐藏（`AiWorkspaceLayout` + `AiSessionContext`）
- **会话详情页右上角不再出现「新建会话」按钮**；新建仅保留侧栏入口
- 原报表/看板页取消顶栏「问 AI」入口
- AI 会话框支持输入 `@`（或点工具栏 @）选择「专属定制」已挂载报表链接，数据与定制页同源（`customReports.ts`）

### 迭代（2026-07-23 18:07）
- 进行中会话：输入框上方不再显示能力标签与提示词（仅空态首页保留）
- 历史会话操作：侧栏「⋯」菜单支持置顶 / 重命名 / 归档 / 删除；置顶会话排在列表前面

### 迭代（2026-07-23 17:55）
- **会话归档**：侧栏新增「会话归档」；会话项可归档；归档页支持搜索 / 恢复 / 删除 / 打开
- **风格统一**：AI 工作台 `ConfigProvider` 主色 `#111827`；CSS 变量统一墨色主题，去掉分散的 Ant 蓝
- **搜索**：侧栏会话、产物仓库、附件仓库均支持关键词搜索（附件支持实时过滤）

## 2026-07-24

### 提问
- 将 supermingzhao 修改合并到 shujie；隐藏归档；Agent 仅保留「即时零售」「周报生成」；隐藏产物仓库

### 处理记录
- 已合并 `supermingzhao` → `shujie`
- 隐藏会话归档入口与归档操作
- Agent 能力标签精简为「即时零售」「周报生成」
- 隐藏产物仓库入口与路由入口
